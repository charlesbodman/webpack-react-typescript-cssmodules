import * as React from 'react';

import * as headerStyles from './header.css';

export default class Header extends React.Component
{
    render(){
        return <div className={headerStyles.header} >Header</div>
    }
}